
This git repo contains a short ansible playbook fragment to
manage my personal vim settings and various home directory
dotfiles.

Example of overriding Ansible vars at runtime:

ansible-playbook mac.yml -e "blocke_mail_addr=blocke@newpaltz.edu"

