
git clone https://github.com/blocke/dotfiles.git dotfiles
cd dotfiles
git submodule init
git submodule update

apt-get install vim tmux exuberant-ctags keychain python-virtualenv
pip install flake8

git config --global core.excludefile ~/.gitignore

