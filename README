
git clone https://github.com/blocke/dotfiles.git dotfiles
cd dotfiles
git submodule init
git submodule update

apt-get install vim tmux exuberant-ctags keychain python-virtualenv python-dev
pip install flake8

apt-get install npm
npm install jshint -g

git config --global core.excludesfile ~/.gitignore

